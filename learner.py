import json
import numpy as npy
import matplotlib.pyplot as pyplot
from chainer import FunctionSet, Variable, optimizers, serializers 
import chainer.functions as cf
import chainer.links as cl 

 class Learner:
 
	def __init__(self):
	
	def __testing__(self):
		a = Variable(npy.array([3], dtype=npy.float32))
		b = Variable(npy.array([4], dtype.npy.float32))
		c = a**2 + b**c 